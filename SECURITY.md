# 安全政策

## 报告安全问题

发现本仓库内容可能导致的安全问题（如某 SOP 引导用户暴露密钥、脚本注入风险），请**不要**开公开 Issue：
发邮件至仓库所有者（见 GitHub 主页联系方式），72 小时内回复。

## 本项目对「客户数据与密钥」的立场（设计原则，欢迎监督）

1. **客户数据永不入库**：`.gitignore` 排除在办客户夹/归档/工作区；任何人 fork 本仓库不应获得任何真实客户信息。
2. **密钥不进交付包**：交付包（hermes/）只含工作流与人格定义；API key 一律放客户机器本地的 `~/.hermes/.env`，部署时现场配置，不落文档、不落 git。
3. **最小权限**：交付包默认配置 allowed_users=仅客户本人；支付/安全类技能不装给小白客户（见 `03-SOP/c2-方案设计.md` 安全基线）。
4. **自进化有护栏**：所有客户 agent 随包携带《进化守则》（禁区+报告义务），这是对抗「agent 越用越歪」的机制设计。

发现的违规案例（包括我们自己的提交）请直接 Issue 通知，我们会立即处理并在 CHANGELOG 说明。
