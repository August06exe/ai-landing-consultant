# 安全政策

## 报告安全问题

发现本仓库内容可能导致的安全问题（如某 SOP 引导用户暴露密钥、脚本注入风险），请**不要**开公开 Issue：
发邮件至仓库所有者（见 GitHub 主页联系方式），72 小时内回复。

## 客户信息防泄露·三层防线（本项目对贡献者与使用者的承诺）

「开源的是产线，客户的永远归客户。」这条原则靠机制保证，不靠自觉：

### 第一层：结构隔离（gitignore）
`05-客户/*`（模板除外）、`06-归档/`、`07-工作区/` 全部默认排除；真实客户档案、报价、录屏在磁盘上存在，但 git 看不见它们。

### 第二层：提交门卫（pre-commit 钩子）
`scripts/hooks/pre-commit` 在**每次提交时**扫描暂存区：客户数据路径、手机号、API key 特征（sk-/ghp_/AKIA…）、私钥文件一律拦截。启用方式（每个克隆做一次）：

```bash
git config core.hooksPath scripts/hooks
```

> 紧急越过大门：`ALLOW_RISKY_COMMIT=1 git commit …`——存在这个逃生阀是为了掩码示例误报，**用它提交真实客户数据=违反本政策**。

### 第三层：全量体检（审计脚本）
```bash
bash scripts/check-privacy.sh
```
扫 git 全部历史 + 当前跟踪文件，出 PASS/FAIL 报告。推送前必跑。

### 服务商（仓库所有者）侧的补充动作

- GitHub 仓库 Settings → Code security：开启 **Secret scanning** 与 **Push protection**（公钥泄露的最后一道网）
- 一旦真实密钥/客户数据进了公开历史：**作废轮换密钥**是唯一正解（删提交没用，历史已在别人缓存里）
- 云盘/同步盘注意：本项目文件夹若被 OneDrive/网盘同步，客户数据同样会离开本机——建议把 `05-客户/` 排除在同步盘之外

## 本项目对「密钥管理」的立场（设计原则）

1. 交付包（hermes/）只含工作流与人格定义；API key 一律放客户机器本地 `~/.hermes/.env`，部署时现场配置，不落文档、不落 git。
2. 最小权限：交付包默认 allowed_users=仅客户本人；支付/安全类技能不装给小白客户（见 `03-SOP/c2-方案设计.md` 安全基线）。
3. 自进化有护栏：所有客户 agent 随包携带《进化守则》（禁区+报告义务）。

发现的违规案例（包括我们自己的提交）请直接 Issue 通知，我们会立即处理并在 CHANGELOG 说明。
