# 专题12：交付包指令核对清单（全量断言 vs 官方文档）

> 版本 1.0 ｜ 核对日期 2026-09-18（夜航）｜ 核对人：夜航工程师
> 方法：扫描 `03-SOP/` 全部 13 文件、`01-顾问agent/提示词.md`、四案例全套、`05-客户/_模板/`、`02-知识库/05-配方库/` 中**每一条** hermes 命令/配置/路径断言，逐条对照 `02-知识库/02-官方文档/llms-full.txt`（2026-09-15 抓取，行号 2026-09-18 实测）判定。
> 判定图例：✅ 与文档一致（附行号）｜✗ 与文档冲突（**已当场修源文件+升版本**）｜⚠ 存疑/行号漂移（已注明处置）

## 一、核对结果总览

| 统计 | 数量 |
|---|---|
| 正文判定行（命令/配置/路径断言） | 38 行 |
| ✅ 通过 | 25 |
| ✗ 冲突（已当场修源） | **11**（10 处本次纠错 + 1 处专题02 存量勘误 HERMES_HOME） |
| ⚠ 存疑（已标注处置） | 2 |
| 联动修订源文件 | **11 个**（c3/c4/c5、专题02、模板×3、四案例×4） |
| 同源根因 | AGENTS/守则落位 ×6（模板00+四案例4+守则载体）、token 硬顶 ×2（c4+c5）、其余 3 独立 |
| 抽样未覆盖（记入未竟） | 案例 wf-*.md 逐工具名断言 |

**当日纠错数：✗ 11 处，联动修订 11 个源文件**（c3 v2.1 / c4 v2.1 / c5 v2.1 / 专题02 v1.1 / 模板 00-自装配指令 v1.1 / 模板 AGENTS v1.1 / 模板进化守则 v1.1 / 四案例 00-自装配指令 ×4）。

## 二、03-SOP 逐条核对

### c3-交付包制作

| 断言 | 判定 | 依据/处置 |
|---|---|---|
| 「SOUL.md 写入 `~/.hermes/`」 | ✅ | SOUL.md 住 `~/.hermes/SOUL.md`，家目录人格文件（llms:87854） |
| 「安装技能清单（`hermes skills install …`）」 | ✅ | `hermes skills install openai/skills/k8s`（:511、:11559）；装前自动安全扫描（:511） |
| 「人格语气进 SOUL，任务/路径/流程进 AGENTS」分流 | ✅ | Which File Does What 主表（:87852-87858）；SOUL 管 who you are，AGENTS 管 project needs（:87864-87869） |
| 引用「llms-full.txt:13851-13990」 | ⚠→已修 | 行号漂移：Personality & SOUL.md 章实为 :13914 起。v2.1 改引 :13914-14010 + :87844-87896 |
| 施工顺序含「配置 home channel」 | ✅ | 渠道机制成立（gateway 单进程连全部平台，:24608） |

### c4-部署引导

| 断言 | 判定 | 依据/处置 |
|---|---|---|
| 「`hermes setup`（或 Desktop 内引导）通第一个模型」 | ✅ | `hermes setup` 完整向导（:80、:587）；Desktop 首启 onboarding（:70596） |
| 「设 allowed_users（只允许客户自己）」 | ✅→精确化 | 平台级 `allowed_users` 白名单机制属实（Matrix :29916；Nostr 默认私模式 :79846）。v2.1 精确为「渠道白名单（各平台 allowed_users）」 |
| 「按 c2 参数设 token 月度硬顶与超限提醒」 | ✗→已修 | **官方无原生月度花费硬顶配置**。存在的相邻机制：`max_turns`（:4819）、500 轮迭代预算（:4751）、delegation 深度帽（:6369）、dashboard Usage analytics 事后统计（:6517）、`/usage` 查询（:24829）、cron 配置校验失败不烧 token（:15459-15461）。v2.1 改「组合拳」：用量可见+辅助槽降档+守则约束+d4 巡检 |
| 「杀毒软件…已知误报墙位，见专题02」 | ✗→已修 | 引用悬空：专题02 v1.0 无杀软内容。已补专题02「杀软墙位」节（依据 :834、:968），c4 v2.1 引用落点同步 |
| 「官网 Desktop 安装包，下一步到底，免管理员权限」 | ✅ | Windows 薄 GUI 安装器底层调 install.ps1（:87943）；PowerShell 路径无需管理员（:87921） |

### c5-自装配验证

| 断言 | 判定 | 依据/处置 |
|---|---|---|
| 「`hermes skills list` 对清单」 | ✅ | `hermes skills list --source hub` 等子命令存在（:11565） |
| 「仪表盘可见 token 硬顶」 | ✗→已修 | 同 c4：无原生硬顶。v2.1 改「打开 dashboard 用量分析」（Usage analytics，:6517） |
| 「问它『你现在用的是什么模型』」 | ✅ | 模型可查询成立（composer 选择器/Settings → Model，:70511-70512） |

### d2-故障分诊

| 断言 | 判定 | 依据/处置 |
|---|---|---|
| 「网关没跑：启动 `hermes gateway`」 | ✅ | `hermes gateway` 启动网关（:591） |
| 「每次 `hermes update` 后必做」回归 | ✅ | `hermes update` 流程+官方五步验证（:786、:996-1004） |
| 「`hermes skills list` 对清单（更新后）」 | ✅ | 同上（:11565）；`hermes update` 会新增 bundled skills（:10916） |

## 三、四案例逐条核对

| 文件 | 断言 | 判定 | 处置 |
|---|---|---|---|
| 投资人/部署执行单:16 | `hermes setup` 接 GLM，主档+辅助槽 key 配进 `~/.hermes/.env` | ✅ | 密钥进 `.env`、非密配置进 config.yaml（:369-372）；GLM 官方 provider（:321 `GLM_API_KEY`/`ZAI_API_KEY`）；辅助槽机制（:6569+） |
| 投资人/00-自装配指令:12 | 「把 AGENTS.md 写入 `~/.hermes/AGENTS.md`」 | ✗ | AGENTS.md 属**项目工作目录**文件，从 CWD 加载+子目录渐进发现（:13548、:13566）；`~/.hermes/` 下只住 SOUL.md。已改「放到你的工作目录（用户主目录）」 |
| 拉面店/00-自装配指令:12 | 同上 | ✗ | 同上已修 |
| 诊所/00-自装配指令:14 | 同上 | ✗ | 同上已修 |
| 财务/00-自装配指令:12 | 同上 | ✗ | 同上已修 |
| 财务/00-交付说明:13 | 「`hermes skills list` 核对为空（0 外部技能）」 | ✅ | :11565 |
| 财务/部署执行单:16 | `hermes setup` 接 DeepSeek | ✅ | DeepSeek 官方 provider（:350 `DEEPSEEK_API_KEY`） |
| 诊所/部署执行单:15 | `hermes setup` 接本地模型 base_url=本机 11434 | ✅ | Custom Endpoint（:355「VLLM, SGLang, Ollama, or any OpenAI-compatible API」）；11434 为 Ollama 默认端口；本地模型指南齐备（:79141+） |
| 四案例 cron 类指令（对 agent 说/写 cron.md） | ✅ | 自然语言建 cron 走 `cronjob` 工具（:15439-15445）；cron 会话禁递归建 cron（:15411-15413）——**交付包 cron.md 让 agent 逐条建是安全路径** |

## 四、模板与配方库逐条核对

| 文件 | 断言 | 判定 | 处置 |
|---|---|---|---|
| 模板/00-自装配指令:18 | SOUL.md 写入 `~/.hermes/SOUL.md`（存在则先确认） | ✅ | :87854；官方「existing files are never overwritten」——模板加确认步是稳妥增强 |
| 模板/00-自装配指令:22 | 「AGENTS.md…放在 `~/.hermes/` 下」 | ✗→已修 | 同案例。v1.1 改「放到工作目录（用户主目录或 --cwd 指定目录；不是 `~/.hermes/`）」 |
| 模板/00-自装配指令:26 | `hermes skills install official/<分类>/<技能名>` | ✅ | `official` 是合法 hub source：官方示例 `hermes skills install official/security/1password`（:11560） |
| 模板/00-自装配指令:28 | `hermes skills list` 验证 | ✅ | :11565 |
| 模板/进化守则头部 | 「装入 ~/.hermes/（并入 AGENTS.md 或独立成文件）」 | ✗→已修 | `~/.hermes/` 下独立 md 不会每会话自动加载（自动加载=SOUL.md+记忆快照+工作目录 AGENTS 链，:13548-13562）。v1.1 改「并入 SOUL.md 尾部或并入 AGENTS.md」 |
| 模板/SOUL 头部 | 「SOUL.md 只放人格」 | ✅ | :87864「SOUL.md is who the agent *is*」 |
| 配方库02:18 | cron 表达式 `0 8 * * *` | ✅ | 「natural language or cron expressions」（:15379） |
| 配方库03:22 | 装 Desktop → `hermes setup`；.env 直接拷则跳过配 key | ✅ | .env 可迁移（HERMES_HOME 数据目录整体可拷，:88104「Survives reinstalls」；backup/import 含凭据 :94） |
| 根 README:121（行动项） | `hermes plugins install` 一条命令装顾问 | ✅ | `hermes plugins install owner/repository --enable/--no-enable`（:2810、:14374） |

## 五、专题02 v1.0 存量行号勘误（已在 v1.1 修正）

| v1.0 引用 | v1.1 实测 | 内容 |
|---|---|---|
| :87701-87713 | :88003-88009 | Git Bash 解析顺序 |
| :87717-87724 | :88015-88028 | UTF-8 stdio |
| :87682-87699 | :87984-87996 | 功能矩阵 |
| :69454 | :87976-87978 | Windows 最高摩擦点=逐工具配 key <!-- anchor-ok: 勘误表历史值 --> |
| :6471 | :6505 | Portal 订阅 10% off |
| 「家目录 `%USERPROFILE%\.hermes`」 | ✗→已修 | Windows 原生安装器设 `HERMES_HOME=%LOCALAPPDATA%\hermes`（:88106、:87973） |
| 「15+渠道」 | 更新 | 平台对照表 28 行（:24632-24661），v1.1 改「20+渠道」保守口径 |

> 根因说明：专题02 v1.0 的行号来自 2026-09-16 会话（可能引用了当时抓取的中间版本或手工转抄误差）。教训入库：**行号必须当日 grep 实测，禁止从旧文档转抄**（已写进 c3 自检清单既有条款的执行理由）。

## 六、未验证清单（查不到/未覆盖，禁止当事实引用）

1. **案例 wf-\*.md 内的逐条工具名断言**（如「调用 web_search」「用 read_file」）：数量大（四案例约 20 个工作流文件），本次抽样未逐条核。处置：下次单子用到哪条核哪条；或立内容 lint 脚本（专题09 行动项 7）批量核。
2. **安装期杀软白名单具体步骤**：llms-full.txt 无（仅更新期 :834/:968）。hermes-agent GitHub README 的 Defender 节未重核原文。标「未验证」。
3. **Desktop 默认工作目录具体值**（app 图形界面打开时的 CWD）：文档只给 `--cwd`/`HERMES_DESKTOP_CWD` 覆盖手段（:70518），未写默认值。模板已用「用户主目录」保守表述+验证动作兜底。
4. **`hermes setup` 向导的逐屏交互文案**：文档给三模式与结果（:302-309），未给逐屏截图级描述。专题11 已按「看到什么点什么」合理推演，界面文案以实机为准。

## 七、核对方法论沉淀（给下一次核对的人）

1. **先 grep 后读**：命令断言用 `grep -n "hermes [a-z-]"` 提取，路径断言用 `~/.hermes|%LOCALAPPDATA%` 提取，比通读快 10 倍且不漏。
2. **官方示例即证据**：判定一条命令是否存在，找官方文档里的**原样示例行**（如 :11560），不靠「应该有」。
3. **同源错误连坐排查**：模板错一处，四案例+模板必同查（本次 AGENTS.md 落位即同源 ×5）。
4. **修源文件必升版本+留变更记录**；错误本身记进核对表（错误也是资产——防止下次改回去）。

> 变更记录：v1.1 2026-09-18 顾问审校轮——统计口径统一（38 判定行=25✅/11✗/2⚠，11 个修订源文件，同源根因 2 组）；消除「7 处/8 文件」旧口径与正文表格的矛盾。
> 变更记录：v1.0 2026-09-18 夜航首次产出——全量扫描 6 类交付物，逐条判定；✗ 当场修源+升版本；未验证清单 4 项立此存照。
